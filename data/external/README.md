# Datos externos

Carpeta para fuentes de terceros que no se versionan en Git (API keys, dumps adicionales, datasets descargados manualmente).

Los datos principales del proyecto están en `data/raw/` (CSV de contaminantes) y se complementan con la API Open-Meteo en tiempo de extracción.
